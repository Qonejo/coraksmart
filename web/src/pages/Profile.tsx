import React from 'react';

const Profile = () => {
  return (
    <div className="bg-gray-900 text-white min-h-screen flex items-center justify-center font-mono">
      <h1 className="text-4xl">Página de Perfil (En construcción)</h1>
    </div>
  );
};

export default Profile;
